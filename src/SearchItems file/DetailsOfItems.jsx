

function DetailsOfItems({ loader }) {

    const { id, image, name, title, details, location, countryName, placeName } = loader

    return (
        <div>

        </div>
    )
}

export default DetailsOfItems
